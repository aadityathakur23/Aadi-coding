// Write a program to take three variables to store your birth date, birth month, and birth year, respectively, and then print them one by one in a specified order.
const input = require("readline-sync");
let a = input.questionInt("the birth date: ");
let b = input.question(" Enter your birth month: ");
let c = input.question(" Enter your birth year: ");
console.log(a)
console.log(b)
console.log(c)